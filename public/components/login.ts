// public/components/login.ts

/*
Fil som innehåller UI-logik/widgets och funktioner.
*/

/*
Syftet är att strukturera upp hela vyer (views) i minddre beståndsdelar (widgets) för bättre översikt.
*/

/*
export function renderLogin() {
  const form = document.createElement("form");
  form.addEventListener("submit", handleLogin);
  document.querySelector("#app .login-box").appendChild(form);
}
*/
